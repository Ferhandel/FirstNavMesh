using UnityEditor;
using UnityEngine;

[CustomEditor(typeof(Enemy))]
public class FieldOvViewEditor : Editor
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
